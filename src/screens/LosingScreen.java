package screens;

import io.ResourceFinder;

/**
 * Losing Screen Class for Lumbearjack.
 *
 */
public class LosingScreen extends MainScreen
{
  /**
   * Losing Screen Constructor.
   * @param width the width of the screen.
   * @param height the height of the screen.
   * @param rf ResourceFinder for pngs.
   */
  public LosingScreen(final int width, final int height, final ResourceFinder rf)
  {
    super(width, height, rf);
  }
}
